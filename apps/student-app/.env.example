# Local: leave empty — Vite proxies /api → http://127.0.0.1:4000
# Production (Netlify env):
VITE_API_URL=https://api.avichian.in

# Optional Appwrite (if used for SMS OTP)
# VITE_APPWRITE_ENDPOINT=https://cloud.appwrite.io/v1
# VITE_APPWRITE_PROJECT_ID=

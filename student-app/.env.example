# Local development: leave empty — Vite proxies /api and /socket.io to the backend.
VITE_API_URL=

# Optional: direct backend (includes /api suffix)
# VITE_API_URL=http://localhost:4000/api

# Production builds use .env.production or public/config.json { "apiUrl": "https://…" }
